[tool.ruff.lint]
ignore = ["DTZ"]

[tool.ruff.lint.per-file-ignores]
"alembic/versions/*.py" = ["F401"]
"alembic/env.py" = ["F401"]

[tool.mypy]
plugins = ["pydantic.mypy"]

[[tool.mypy.overrides]]
module = "alembic.*"
ignore_errors = true
